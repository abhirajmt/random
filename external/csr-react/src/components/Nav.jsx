import * as React from 'react';

const Nav = () => {
  return (
    <nav
      style={{
        background: 'yellow',
        width: '100%',
      }}
    >
      external csr React App
    </nav>
  );
};

export default Nav;
